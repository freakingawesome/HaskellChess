module Hchess.Game.GameSpec where

import SpecHelper
import qualified Data.Map as Map
import Data.Maybe(fromJust)
import Data.List(sort)

spec :: Spec
spec = do
  return ()
  -- describe "Dummy describe" $ do
    -- it "can move to either d3 or d4 if isolated" $ do
      -- True `shouldBe` True
